/**
 * Stub: Capacitor was removed, so push notifications are a no-op on the web.
 * Re-implement when wrapping the app with Capacitor for Google Play.
 */
export function usePushRegistration() {
  // no-op on web
}
